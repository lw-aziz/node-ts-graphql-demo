type Mutation {
  createEvent(data: CreateEventInput!): EventData @isAuthenticated
}

input CreateEventInput {
  title: String!
  description: String!
  eventDate: DateTime!
}

type EventData {
  id: String!
  title: String!
  description: String!
  eventDate: DateTime!
  createdAt: DateTime!
}
